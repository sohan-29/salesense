# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Trained model artifacts (regenerable from Atlas data)
artifacts/
*.joblib
*.pkl

# MLflow tracking store (regenerable via `python -m recommender.cli mlflow-run`)
mlflow.db
mlflow.db-journal
mlruns/

# Local venv
venv/
.venv/

# Env / secrets (already covered by root .gitignore, repeated for safety)
.env
.env.*
